From 280c389c264cc2538a97e78df04c81f2daab6a1f Mon Sep 17 00:00:00 2001
From: Patch Generator <patch@example.com>
Date: Mon, 20 Jul 2026 14:13:41 +0000
Subject: [PATCH] modularize: split emergency LLM proxy shutdown into assembly
 + 6 modules

---
 docs/developer-lightspeed-guide/master.adoc   |   2 +-
 ...assembly_emergency-llm-proxy-shutdown.adoc |  22 ++
 .../con_llm-proxy-shutdown-overview.adoc      |  25 +++
 .../proc_disable-llm-proxy-cli.adoc           |  53 +++++
 .../proc_disable-llm-proxy-web-console.adoc   |  41 ++++
 .../proc_emergency-llm-proxy-shutdown.adoc    | 194 ------------------
 .../proc_reenable-llm-proxy.adoc              |  44 ++++
 .../proc_validate-llm-proxy-shutdown.adoc     |  32 +++
 .../ref_llm-proxy-developer-impact.adoc       |  19 ++
 9 files changed, 237 insertions(+), 195 deletions(-)
 create mode 100644 docs/topics/developer-lightspeed/assembly_emergency-llm-proxy-shutdown.adoc
 create mode 100644 docs/topics/developer-lightspeed/con_llm-proxy-shutdown-overview.adoc
 create mode 100644 docs/topics/developer-lightspeed/proc_disable-llm-proxy-cli.adoc
 create mode 100644 docs/topics/developer-lightspeed/proc_disable-llm-proxy-web-console.adoc
 delete mode 100644 docs/topics/developer-lightspeed/proc_emergency-llm-proxy-shutdown.adoc
 create mode 100644 docs/topics/developer-lightspeed/proc_reenable-llm-proxy.adoc
 create mode 100644 docs/topics/developer-lightspeed/proc_validate-llm-proxy-shutdown.adoc
 create mode 100644 docs/topics/developer-lightspeed/ref_llm-proxy-developer-impact.adoc

diff --git a/docs/developer-lightspeed-guide/master.adoc b/docs/developer-lightspeed-guide/master.adoc
index 13e0556..e0cea89 100644
--- a/docs/developer-lightspeed-guide/master.adoc
+++ b/docs/developer-lightspeed-guide/master.adoc
@@ -6,6 +6,6 @@ include::topics/developer-lightspeed/assembly_run-dev-lightspeed-analysis.adoc[l
 
 include::topics/developer-lightspeed/con_developer-lightspeed-logs.adoc[leveloffset=+1]
 
-include::topics/developer-lightspeed/proc_emergency-llm-proxy-shutdown.adoc[leveloffset=+1]
+include::topics/developer-lightspeed/assembly_emergency-llm-proxy-shutdown.adoc[leveloffset=+1]
 
 :!mta-developer-lightspeed:
diff --git a/docs/topics/developer-lightspeed/assembly_emergency-llm-proxy-shutdown.adoc b/docs/topics/developer-lightspeed/assembly_emergency-llm-proxy-shutdown.adoc
new file mode 100644
index 0000000..04b7126
--- /dev/null
+++ b/docs/topics/developer-lightspeed/assembly_emergency-llm-proxy-shutdown.adoc
@@ -0,0 +1,22 @@
+// Module included in the following assemblies:
+//
+// * docs/developer-lightspeed-guide/master.adoc
+
+:_mod-docs-content-type: ASSEMBLY
+
+[id="assembly-emergency-llm-proxy-shutdown_{context}"]
+= Shutting down the LLM proxy for emergency system isolation
+
+During a security incident, stop all outbound large language model (LLM) traffic from developer integrated development environments (IDEs).
+
+include::con_llm-proxy-shutdown-overview.adoc[leveloffset=+1]
+
+include::proc_disable-llm-proxy-web-console.adoc[leveloffset=+1]
+
+include::proc_disable-llm-proxy-cli.adoc[leveloffset=+1]
+
+include::ref_llm-proxy-developer-impact.adoc[leveloffset=+1]
+
+include::proc_reenable-llm-proxy.adoc[leveloffset=+1]
+
+include::proc_validate-llm-proxy-shutdown.adoc[leveloffset=+1]
diff --git a/docs/topics/developer-lightspeed/con_llm-proxy-shutdown-overview.adoc b/docs/topics/developer-lightspeed/con_llm-proxy-shutdown-overview.adoc
new file mode 100644
index 0000000..bbe4165
--- /dev/null
+++ b/docs/topics/developer-lightspeed/con_llm-proxy-shutdown-overview.adoc
@@ -0,0 +1,25 @@
+// Module included in the following assemblies:
+//
+// * docs/developer-lightspeed-guide/master.adoc
+
+:_mod-docs-content-type: CONCEPT
+
+[id="llm-proxy-shutdown-overview_{context}"]
+= LLM proxy shutdown overview
+
+[role="_abstract"]
+The procedure for shutting down the LLM proxy differs depending on how {ProductShortName} is deployed.
+
+[WARNING]
+====
+Perform these steps only under the direction of your security or incident-response team. Disabling the proxy immediately severs all in-progress LLM sessions for every connected developer without warning.
+====
+
+Centralized configuration mode:: In Operator-managed or {ocp-short} deployments, the {ProductShortName} Operator manages the LLM proxy lifecycle. You disable the proxy by setting `kai_llm_proxy_enabled: false` in the Tackle custom resource (CR). The Operator then terminates the proxy pod and does not re-create it.
++
+[IMPORTANT]
+====
+Do not directly scale the proxy `Deployment` object to zero replicas. The {ProductShortName} Operator reconciles managed resources and re-creates any manually scaled-down pods. To permanently disable the proxy, edit the Tackle CR.
+====
+
+Standalone mode:: In standalone mode, there is no central proxy component to disable. Each developer IDE connects directly to the configured public LLM endpoint. Network administrators must block this traffic at the corporate firewall or network policy layer.
diff --git a/docs/topics/developer-lightspeed/proc_disable-llm-proxy-cli.adoc b/docs/topics/developer-lightspeed/proc_disable-llm-proxy-cli.adoc
new file mode 100644
index 0000000..835ce2a
--- /dev/null
+++ b/docs/topics/developer-lightspeed/proc_disable-llm-proxy-cli.adoc
@@ -0,0 +1,53 @@
+// Module included in the following assemblies:
+//
+// * docs/developer-lightspeed-guide/master.adoc
+
+:_mod-docs-content-type: PROCEDURE
+
+[id="disable-llm-proxy-cli_{context}"]
+= Disabling the LLM proxy by using the CLI
+
+For scripted incident response or when the {ocp-short} web console is unavailable, disable the LLM proxy by using the OpenShift CLI (`oc`).
+
+.Prerequisites
+
+* You have installed the OpenShift CLI (`oc`).
+* You are logged in to the cluster with the `cluster-admin` role.
+
+.Procedure
+
+. Patch the Tackle CR to disable the LLM proxy:
++
+[source,terminal,subs="+quotes"]
+----
+$ *oc patch tackle __<tackle_instance_name>__ -n __<mta_namespace>__ \
+  --type=merge -p '{"spec":{"kai_llm_proxy_enabled":false}}'*
+----
++
+.Example
+[source,terminal,subs="+quotes"]
+----
+$ *oc patch tackle mta -n openshift-mta \
+  --type=merge -p '{"spec":{"kai_llm_proxy_enabled":false}}'*
+----
++
+.Expected output
+[source,terminal]
+----
+tackle.tackle.konveyor.io/mta patched
+----
+
+.Verification
+
+. Verify that no proxy pods are running:
++
+[source,terminal,subs="+quotes"]
+----
+$ *oc get pods -n __<mta_namespace>__ | grep proxy*
+----
++
+.Expected output
+[source,terminal]
+----
+No resources found in mta namespace.
+----
diff --git a/docs/topics/developer-lightspeed/proc_disable-llm-proxy-web-console.adoc b/docs/topics/developer-lightspeed/proc_disable-llm-proxy-web-console.adoc
new file mode 100644
index 0000000..eac3c7b
--- /dev/null
+++ b/docs/topics/developer-lightspeed/proc_disable-llm-proxy-web-console.adoc
@@ -0,0 +1,41 @@
+// Module included in the following assemblies:
+//
+// * docs/developer-lightspeed-guide/master.adoc
+
+:_mod-docs-content-type: PROCEDURE
+
+[id="disable-llm-proxy-web-console_{context}"]
+= Disabling the LLM proxy by using the {ocp-short} web console
+
+Disable the LLM proxy in centralized configuration mode by editing the Tackle custom resource (CR) in the {ocp-short} web console.
+
+.Prerequisites
+
+* You are logged in to the {ocp-short} web console with the `cluster-admin` role or an equivalent role that grants write access to the MTA namespace.
+* You have identified the namespace where {ProductShortName} is installed, for example, `mta` or `openshift-mta`.
+
+.Procedure
+
+. In the {ocp-short} web console, switch to the *Administrator* perspective.
+. Click *Operators* > *Installed Operators*.
+. From the *Project* list, select the {ProductShortName} namespace, for example, `openshift-mta`.
+. Click *Migration Toolkit for Applications*.
+. Click the *Tackle* tab.
+. Click the name of your Tackle instance, for example, `mta`.
+. Click *Actions* > *Edit Tackle*.
+. In the YAML editor, find the `spec` section and set the `kai_llm_proxy_enabled` field to `false`:
++
+[source,yaml]
+----
+spec:
+  kai_llm_proxy_enabled: false
+----
+. Click *Save*.
++
+The Operator reconciles the change and terminates the proxy pod.
+
+.Verification
+
+. Click *Workloads* > *Pods*.
+. From the *Project* list, select the {ProductShortName} namespace.
+. Verify that no pod with `proxy` in the name has a `Running` status.
diff --git a/docs/topics/developer-lightspeed/proc_emergency-llm-proxy-shutdown.adoc b/docs/topics/developer-lightspeed/proc_emergency-llm-proxy-shutdown.adoc
deleted file mode 100644
index 4812444..0000000
--- a/docs/topics/developer-lightspeed/proc_emergency-llm-proxy-shutdown.adoc
+++ /dev/null
@@ -1,194 +0,0 @@
-// Module included in the following assemblies:
-//
-// * docs/developer-lightspeed-guide/master.adoc
-
-:_template-generated: 2026-07-05
-
-:_mod-docs-content-type: PROCEDURE
-
-[id="emergency-system-isolation-llm-proxy-shutdown_{context}"]
-= Shutting down the LLM proxy for emergency system isolation
-
-[role="_abstract"]
-During a security incident, stop all outbound large language model (LLM) traffic from developer integrated development environments (IDEs). 
-
-The procedure for shutting down the LLM proxy for emergency system isolation differs for centralized configuration and standalone mode.
-
-[WARNING]
-====
-Perform these steps only under the direction of your security or incident-response team. Disabling the proxy immediately severs all in-progress LLM sessions for every connected developer without warning.
-====
-
-[id="centralized-config-mode-shutdown_{context}"]
-== Centralized configuration mode (Operator or Red{nbsp}Hat OpenShift deployments)
-
-In centralized deployments, the {ProductShortName} Operator manages the LLM proxy. To disable the proxy, you must set `kai_llm_proxy_enabled: false` in the Tackle custom resource (CR). The Operator then terminates the proxy and does not re-create it.
-
-[IMPORTANT]
-====
-Do not directly scale the proxy `Deployment` object to zero replicas. The {ProductShortName} Operator reconciles managed resources and re-creates any manually scaled-down pods. To permanently disable the proxy, edit the Tackle CR.
-====
-
-[id="disable-proxy-web-console_{context}"]
-=== Disabling the LLM proxy by using the {ocp-short} web console
-
-.Prerequisites
-* You are logged in to the Red{nbsp}Hat {ocp-short} web console with the `cluster-admin` role or an equivalent role that grants write access to the MTA namespace.
-* You have identified the namespace where {ProductShortName} is installed, for example, `mta` or `openshift-mta`.
-
-.Procedure
-
-. In the {ocp-short} web console, switch to the *Administrator* perspective.
-. Click *Operators* > *Installed Operators*.
-. From the *Project* list, select the {ProductShortName} namespace, for example, `openshift-mta`.
-. Click *Migration Toolkit for Applications*.
-. Click the *Tackle* tab.
-. Cclick the name of your Tackle instance, for example, `mta`.
-. Click *Actions* > *Edit Tackle*.
-. In the YAML editor, find the `spec` section and set the `kai_llm_proxy_enabled` field to `false`:
-+
-[source,yaml]
-----
-spec:
-  kai_llm_proxy_enabled: false
-----
-. Click *Save*.
-+
-The Operator reconciles the change and terminates the proxy pod.
-
-.Verification
-. To verify that no proxy pods are running, click *Workloads* > *Pods*.
-. From the *Project* list, select the {ProductShortName} namespace.
-. Verify that no pod with `proxy` in the name has a `Running` status.
-
-[id="disable-proxy-cli_{context}"]
-=== Disabling the LLM proxy by using the CLI
-
-For scripted incident response or when the web console is unavailable, disable the LLM proxy by using the command-line interface (CLI).
-
-.Prerequisites
-* You have installed the OpenShift CLI (`oc`).
-* You are logged in to the cluster with the `cluster-admin` role.
-* You installed the Red{nbsp}Hat Logging Operator in your cluster.
-
-.Procedure
-
-. Patch the Tackle CR to disable the LLM proxy:
-+
-[source,terminal,subs="+quotes"]
-----
-$ *oc patch tackle __<tackle_instance_name>__ -n __<mta_namespace>__ \
-  --type=merge -p '{"spec":{"kai_llm_proxy_enabled":false}}'*
-----
-+
-.Example
-[source,terminal,subs="+quotes"]
-----
-$ *oc patch tackle mta -n openshift-mta \
-  --type=merge -p '{"spec":{"kai_llm_proxy_enabled":false}}'*
-----
-+
-.Expected output
-[source,terminal]
-----
-tackle.tackle.konveyor.io/mta patched
-----
-
-. Verify that no proxy pods are running:
-+
-[source,terminal,subs="+quotes"]
-----
-$ *oc get pods -n __<mta_namespace>__ | grep proxy*
-----
-+
-.Expected output
-[source,terminal]
-----
-No resources found in mta namespace.
-----
-
-[id="developer-impact_{context}"]
-=== Expected behavior on developer workstations
-
-After the Operator terminates the proxy pod, you can expect the following behaviors on the developer workstations:
-
-* Any in-progress LLM request from an IDE client receives an immediate `503 Service Unavailable` or `connection refused` error.
-* No further LLM requests can be sent until the proxy is re-enabled.
-* Standard IDE functionality, such as code editing, file navigation, and build tools, is unaffected. Only the Developer Lightspeed AI assistant stops responding.
-* Developers see an error indicator in the Lightspeed panel of their IDE. The exact message depends on the IDE client, but it is typically the following message:
-+
-----
-Error: Unable to connect to the LLM proxy service. Contact your administrator.
-----
-
-[id="reenable-proxy_{context}"]
-=== Re-enabling the LLM proxy after an incident
-
-Re-enable the proxy only after your security team confirms that the incident is resolved and the environment is safe.
-
-.Procedure by using the {ocp-short} web console
-
-. Click *Operators* > *Installed Operators*.
-. Select the *Tackle* instance.
-. Click *Actions* > *Edit Tackle*.
-. Set the `kai_llm_proxy_enabled` field to `true`:
-+
-[source,yaml]
-----
-spec:
-  kai_llm_proxy_enabled: true
-----
-. Click *Save*, and wait for the proxy pod to reach the `Running` status.
-
-.Procedure by using the CLI
-
-. Patch the Tackle CR to enable the LLM proxy:
-+
-[source,terminal,subs="+quotes"]
-----
-$ *oc patch tackle __<tackle_instance_name>__ -n __<mta_namespace>__ \
-  --type=merge -p '{"spec":{"kai_llm_proxy_enabled":true}}'*
-----
-
-.Verification
-
-. Verify that a proxy pod is running:
-+
-[source,terminal,subs="+quotes"]
-----
-$ *oc get pods -n __<mta_namespace>__ | grep proxy*
-----
-. Ask a developer to submit a test query from their IDE and confirm that a valid response is returned.
-
-[id="standalone-mode-shutdown_{context}"]
-== Standalone mode (manual desktop setup)
-
-In standalone mode, there is no central proxy component to disable. Each developer IDE connects directly to the configured public LLM endpoint. Network administrators must block this traffic at the corporate firewall or network policy layer.
-
-[id="validate-proxy-shutdown_{context}"]
-== Validating that the LLM connection is terminated
-
-After you apply either the centralized or standalone isolation procedure, perform the following validation to confirm that outbound LLM traffic is fully blocked.
-
-.Procedure
-
-* On the affected workstation, confirm that the endpoint is unreachable:
-+
-[source,terminal,subs="+quotes"]
-----
-$ *curl -v --max-time 10 https://api.openai.com/v1/models*
-----
-+
-.Expected output when isolation is active
-[source,terminal,subs="+quotes"]
-----
-* connect to __<ip>__ port 443 failed: Connection refused
-curl: (7) Failed to connect to api.openai.com port 443 after __<X>__ ms: Connection refused
-----
-+
-Alternatively, in centralized mode where DNS resolves but the service is down, the expected output is as follows:
-+
-[source,terminal]
-----
-curl: (22) The requested URL returned error: 503
-----
diff --git a/docs/topics/developer-lightspeed/proc_reenable-llm-proxy.adoc b/docs/topics/developer-lightspeed/proc_reenable-llm-proxy.adoc
new file mode 100644
index 0000000..64e03c6
--- /dev/null
+++ b/docs/topics/developer-lightspeed/proc_reenable-llm-proxy.adoc
@@ -0,0 +1,44 @@
+// Module included in the following assemblies:
+//
+// * docs/developer-lightspeed-guide/master.adoc
+
+:_mod-docs-content-type: PROCEDURE
+
+[id="reenable-llm-proxy_{context}"]
+= Re-enabling the LLM proxy after an incident
+
+Re-enable the proxy only after your security team confirms that the incident is resolved and the environment is safe.
+
+.Procedure by using the {ocp-short} web console
+
+. Click *Operators* > *Installed Operators*.
+. Select the *Tackle* instance.
+. Click *Actions* > *Edit Tackle*.
+. Set the `kai_llm_proxy_enabled` field to `true`:
++
+[source,yaml]
+----
+spec:
+  kai_llm_proxy_enabled: true
+----
+. Click *Save*, and wait for the proxy pod to reach the `Running` status.
+
+.Procedure by using the CLI
+
+. Patch the Tackle CR to enable the LLM proxy:
++
+[source,terminal,subs="+quotes"]
+----
+$ *oc patch tackle __<tackle_instance_name>__ -n __<mta_namespace>__ \
+  --type=merge -p '{"spec":{"kai_llm_proxy_enabled":true}}'*
+----
+
+.Verification
+
+. Verify that a proxy pod is running:
++
+[source,terminal,subs="+quotes"]
+----
+$ *oc get pods -n __<mta_namespace>__ | grep proxy*
+----
+. Ask a developer to submit a test query from their IDE and confirm that a valid response is returned.
diff --git a/docs/topics/developer-lightspeed/proc_validate-llm-proxy-shutdown.adoc b/docs/topics/developer-lightspeed/proc_validate-llm-proxy-shutdown.adoc
new file mode 100644
index 0000000..8ab54d7
--- /dev/null
+++ b/docs/topics/developer-lightspeed/proc_validate-llm-proxy-shutdown.adoc
@@ -0,0 +1,32 @@
+// Module included in the following assemblies:
+//
+// * docs/developer-lightspeed-guide/master.adoc
+
+:_mod-docs-content-type: PROCEDURE
+
+[id="validate-llm-proxy-shutdown_{context}"]
+= Validating that the LLM connection is terminated
+
+After you apply either the centralized or standalone isolation procedure, confirm that outbound LLM traffic is fully blocked.
+
+.Procedure
+
+* On the affected workstation, confirm that the LLM endpoint is unreachable:
++
+[source,terminal,subs="+quotes"]
+----
+$ *curl -v --max-time 10 https://api.openai.com/v1/models*
+----
++
+.Expected output when isolation is active (standalone mode or DNS-blocked)
+[source,terminal,subs="+quotes"]
+----
+* connect to __<ip>__ port 443 failed: Connection refused
+curl: (7) Failed to connect to api.openai.com port 443 after __<X>__ ms: Connection refused
+----
++
+.Expected output when isolation is active (centralized mode, DNS resolves but service is down)
+[source,terminal]
+----
+curl: (22) The requested URL returned error: 503
+----
diff --git a/docs/topics/developer-lightspeed/ref_llm-proxy-developer-impact.adoc b/docs/topics/developer-lightspeed/ref_llm-proxy-developer-impact.adoc
new file mode 100644
index 0000000..789f5ce
--- /dev/null
+++ b/docs/topics/developer-lightspeed/ref_llm-proxy-developer-impact.adoc
@@ -0,0 +1,19 @@
+// Module included in the following assemblies:
+//
+// * docs/developer-lightspeed-guide/master.adoc
+
+:_mod-docs-content-type: REFERENCE
+
+[id="llm-proxy-developer-impact_{context}"]
+= Expected behavior on developer workstations after proxy shutdown
+
+After the Operator terminates the proxy pod, developers experience the following behaviors:
+
+* Any in-progress LLM request from an IDE client receives an immediate `503 Service Unavailable` or `connection refused` error.
+* No further LLM requests can be sent until the proxy is re-enabled.
+* Standard IDE functionality, such as code editing, file navigation, and build tools, is unaffected. Only the Developer Lightspeed AI assistant stops responding.
+* Developers see an error indicator in the Lightspeed panel of their IDE. The exact message depends on the IDE client, but it is typically the following:
++
+----
+Error: Unable to connect to the LLM proxy service. Contact your administrator.
+----
-- 
2.52.0

